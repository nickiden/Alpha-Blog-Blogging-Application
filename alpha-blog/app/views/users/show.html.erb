<!--
File:         Show.ht2ml.erb
Project:      Alpha - Blog (Groovebone)
Programmer:   Nick Iden
Date:         Sept 9th, 2020
Comments: 
          This is the show user profile page selected based on udemy ruby on rails course.
          Eventually this page will list blogs posted for Groovebone.
-->

<h2 class="text-center mt-4"><%= @user.username %></h2>

<%= gravatar_for @user, size: 200 %>

 <% if logged_in? && @user == current_user %>
     <div class="text-center mt-4">
       <%= link_to "Edit your profile", edit_user_path(@user), class: "btn btn-outline-info" %>
     </div>
 <% end %>

  <h3 class="text-center mt-4">Articles</h3>
  
  <div class="apple_pagination">
    <%= will_paginate @articles, :container => false %>
  </div>
 
  <%= render 'articles/article' %>		
  
  <div class="apple_pagination">
    <%= will_paginate @articles, :container => false %>
  </div> 